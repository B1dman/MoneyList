<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout2"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="4dp"
    android:background="@drawable/line">

    <ImageView
        android:id="@+id/money_methodimage"
        android:layout_width="50dp"
        android:layout_height="35dp"
        android:layout_marginEnd="232dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/money_date"
        app:layout_constraintTop_toBottomOf="@+id/money_date"
        app:layout_constraintVertical_bias="1.0"

        app:srcCompat="@drawable/fork"
        tools:srcCompat="@drawable/fork" />

    <TextView
        android:id="@+id/money_date"
        android:layout_width="283dp"
        android:layout_height="18dp"
        android:layout_marginStart="4dp"
        android:layout_marginTop="4dp"
        android:text="Money date"
        android:textColor="@color/black"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/money_method"
        android:layout_width="214dp"
        android:layout_height="29dp"
        android:text="Money method"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/money_date"
        app:layout_constraintTop_toBottomOf="@+id/money_date"
        app:layout_constraintVertical_bias="1.0" />

    <TextView
        android:id="@+id/money_money"
        android:layout_width="71dp"
        android:layout_height="29dp"
        android:layout_marginEnd="4dp"
        android:layout_marginBottom="4dp"
        android:gravity="right"
        android:text="money"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.977"
        app:layout_constraintStart_toEndOf="@+id/money_method" />

</android.support.constraint.ConstraintLayout>